apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitnoter.fullname" . }}-test-connection"
  labels:
    {{- include "gitnoter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitnoter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
