apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitnoter.fullname" . }}-frontend
  labels:
    {{- include "gitnoter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "gitnoter.selectorLabels" . | nindent 4 }}-frontend